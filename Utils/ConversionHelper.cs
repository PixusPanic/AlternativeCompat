using Terraria.ModLoader;

namespace AlternativeCompat.Utils
{
    public class ConversionHelper
    {


        /*internal static void ConvertTile(int @base, int biomeCon, int converted)
        {
            TileLoader.RegisterConversion(@base, biomeCon, converted);
            //TileLoader.RegisterConversion(converted, biomeCon, @base);
        }

        internal static void ConvertWall(int @base, int biomeCon, int converted)
        {
            WallLoader.RegisterConversion(@base, biomeCon, converted);
            //WallLoader.RegisterConversion(converted, biomeCon, @base);
        }*/
    }
}
